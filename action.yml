name: 'Auto Release Milestone'
description: 'Drafts a GitHub release based on a newly closed milestone'
author: 'zm'
outputs:
  release-url:
    descrtption: 'The URL of the GitHub release that was drafted'
runs:
  using: 'docker'
  image: 'Dockerfile'
